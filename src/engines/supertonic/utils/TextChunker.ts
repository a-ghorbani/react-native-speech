/**
 * Text Chunker Utility for Supertonic
 *
 * Re-exports the shared text chunker.
 * Splits text into manageable chunks for sentence-level TTS processing.
 */

export {TextChunker, type TextChunk} from '../../../utils/TextChunker';
